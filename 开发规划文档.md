# AI深度创作引擎 — 代码开发规划文档

> 本文档是逐步编码的流程指南。每阶段有明确的交付物和验收标准，按顺序执行。
> 开发时以 `CLAUDE.md` 为约束，`tect-design.md` 为具体实现参考，本文档为流程控制。
注意：
1、文档里的代码示例是蓝图不是最终代码，实际开发时以运行和测试的反馈为准
2、每完成一个阶段的代码开发必须停下等我的下一步指令，不能连续开发多个阶段，这是因为我要做代码审查，或许还要修改代码。
3、在获取API的过程中遇到需要API KEY而你不方便操作的情况可以直接暂停进程，指导我去获取API KEY，其他你不方便的事也是同样道理，直接吩咐我就行。
4、我昨天更换了VPN提供商，因此遇到网络问题（保不准啥时候你需要访问外网），尝试新的代理端口：7890
5、每完成一个阶段的开发都要向我确认并请求是否开始下一阶段。此外，执行过程中遇到任何你认为需要向我询问或不清楚的事，都可以直接吩咐我。另外，积极使用联网搜索工具和context7这个MCP，避免模型知识库过时引起bug出现。
---

## 阶段总览

| 阶段 | 名称 | 核心交付 | 预计耗时 |
|------|------|---------|---------|
| P0 | 项目脚手架 + 基础配置 | 前后端空壳能跑、Vite代理通、依赖无冲突 | 0.5天 |
| P1 | 后端骨架：LLM调用 + SSE路由 | 3个SSE接口可curl调通，返回模拟流 | 0.5天 |
| P2 | 前端骨架：状态机 + 路由 + 服务层 | 状态机跑通全流程（mock数据）、页面路由正常 | 1天 |
| P3 | 前后端联调：核心工作流 | 从输入到完成的完整E2E流程可走通 | 1天 |
| P4 | 知识库模块 | 搜索+问答可用 | 0.5天 |
| P5 | 文章仓库 + 风格Skill | CRUD + 风格提炼可用 | 0.5天 |
| P6 | 集成测试 + Bug修复 | 全功能可用、无阻塞性bug | 0.5天 |

---

## P0：项目脚手架 + 基础配置

### 目标

前后端空壳启动不报错，Vite代理到后端可通，依赖版本无冲突。

### 任务清单

1. **创建后端目录结构**
   - `backend/` 下创建：`main.py`、`config.py`、`database.py`、`models.py`、`schemas.py`、`requirements.txt`、`.env`
   - `backend/routers/__init__.py`（空文件，**必须有**，否则 Python 无法 import routers 包）
   - `backend/services/__init__.py`（空文件，**必须有**，同上）
   - `backend/data/` 目录（用于 SQLite）
   - 参照 `tect-design.md` §8.7、§8.8、§11.4

2. **创建前端项目**
   - `npm create vite@latest frontend -- --template react-ts`
   - 安装依赖（`tect-design.md` §10）
   - 配置 `vite.config.ts`、`tailwind.config.ts`、`postcss.config.js`、`tsconfig.json`
   - 创建 `src/styles/index.css`、`src/styles/tiptap.css`
   - 创建入口 `main.tsx`（**必须引入 `@ant-design/v5-patch-for-react-19`，否则 antd + React 19 会报错**）、`App.tsx`（骨架，路由先占位）
   - 参照 `tect-design.md` §10.1、§10.2

3. **创建项目根目录文件**
   - `.gitignore`（参照 `tect-design.md` §11.4）
   - `.env.example`（内容参照 `tect-design.md` §11.4 的 `.env.example`）

4. **后端 `.env` 文件内容**（P0 必须填入，P1 依赖此文件中的 `DEEPSEEK_API_KEY`）
   ```
   DEEPSEEK_API_KEY=your-api-key-here
   DATABASE_URL=sqlite:///data/app.db
   KB_DIR=../knowledge-base
   CORS_ORIGINS=http://localhost:5173
   ```

4. **后端最小启动验证**
   - `main.py` 只含 FastAPI 实例 + CORS 中间件（**补全 `allow_methods=["*"]`、`allow_headers=["*"]`**，原文档 `...` 是占位符）
   - 加一个 `GET /api/health` 端点返回 `{"code": 0, "message": "ok"}`
   - `uvicorn main:app --reload --port 8000` 启动不报错
   - `curl http://localhost:8000/api/health` 返回正常

5. **前端启动验证**
   - `npm run dev` 启动不报错
   - 浏览器访问 `localhost:5173` 看到页面（空白或占位文字即可）
   - 浏览器控制台访问 `localhost:5173/api/health` 经 Vite 代理返回 `{"code": 0}`

6. **依赖冲突检查**
   - `npm ls react` 无版本冲突（React 19 唯一副本）
   - antd 组件（Button、Modal）正常渲染无报错

### 使用 Skill

- **`investigate`**：如果启动时有任何报错，立即用 investigate skill 做根因分析，不要猜测

### 验收标准

- [ ] `uvicorn main:app --reload` 启动无报错
- [ ] `npm run dev` 启动无报错，浏览器打开无白屏
- [ ] `curl localhost:8000/api/health` → `{"code": 0, "message": "ok"}`
- [ ] 浏览器访问 `localhost:5173/api/health`（经 Vite 代理）→ 同上
- [ ] `npm ls react` 只有一个 React 副本
- [ ] antd `<Button>` 渲染正常，控制台无样式警告

---

## P1：后端骨架 — LLM调用 + SSE路由 + 数据库

### 目标

3个SSE接口 + 文章CRUD + 配置/模型/Schema全部就绪，可用 curl 走通完整后端逻辑。

### 任务清单

1. **配置与基础设施**
   - `config.py`：完整配置（API Key、模型名、数据库URL、CORS、KB路径）
   - `database.py`：SQLAlchemy 引擎 + Session + Base
   - `models.py`：Article ORM 模型
   - `schemas.py`：全部 Pydantic 请求模型 + `ArticleOut` 响应模型（含 `to_camel` alias）
   - 启动时 `Base.metadata.create_all(bind=engine)` 自动建表
   - 参照 `tect-design.md` §8.2、§8.5、§8.6、§8.7

2. **LLM 调用封装**
   - `services/llm.py`：`stream_llm` + `call_llm`
   - 使用 `AsyncOpenAI` 客户端，配置来自 `config.py`
   - 参照 `tect-design.md` §8.1

3. **Agent 提示词**
   - `services/prompt_templates.py`：`ARCHITECT_PROMPT`、`FORMAT_PROMPT`、`STYLE_SKILL_PROMPT`
   - `MENTOR_PROMPT`：从 `mentor-agent-prompt.md` 读取
   - **关键**：`mentor-agent-prompt.md` 当前是纯文本输出格式，但前端 `fetchSSE` 期望 `MentorFeedback` JSON。开发时必须在 `mentor-agent-prompt.md` 末尾追加 JSON 输出格式约束，与 `ARCHITECT_PROMPT`/`FORMAT_PROMPT` 的 `"输出格式（严格JSON，不要包裹markdown代码块）"` 对齐。格式如下：
     ```json
     {
       "items": [{"quote": "...", "problemType": "...", "diagnosis": "...", "rewrite": "...", "whyBetter": "...", "priority": "P0|P1|P2"}],
       "summary": "3句话全局总结",
       "nextStep": "下一步建议"
     }
     ```
   - 参照 `tect-design.md` §8.4

4. **SSE 路由（3个）**
   - `routers/skeleton.py`：`POST /api/skeleton/generate`
   - `routers/mentor.py`：`POST /api/mentor/review`
   - `routers/format.py`：`POST /api/format/adapt`
   - 三接口结构一致：`event_stream()` 生成器 → `StreamingResponse`
   - 参照 `tect-design.md` §8.3

5. **非SSE路由**
   - `routers/articles.py`：CRUD 四个端点
   - `routers/style_skill.py`：`POST /api/style-skill/generate`
   - 参照 `tect-design.md` §8.9

6. **注册所有路由**
   - `main.py` 中 `app.include_router()` 注册全部6个路由模块
   - 确认启动时无 import 错误

7. **后端接口手动验证**
   - curl 调用 `POST /api/skeleton/generate`，传入3个字段（各≥20字），验证 SSE 流式返回 + 最终可 JSON.parse 为 `{title, sections}`
   - curl 调用 `POST /api/articles` 保存文章，`GET /api/articles` 列表，`GET /api/articles/1` 详情，`DELETE /api/articles/1` 删除
   - **注意**：此步需要有效的 `DEEPSEEK_API_KEY`，如果暂时没有，先用 mock 数据验证路由结构正确

### 使用 Skill

- **`investigate`**：任何 import 错误、路由404、SSE格式异常，立即用 investigate 做根因分析

### 验收标准

- [ ] `uvicorn main:app --reload` 启动无报错，所有6个路由模块注册成功
- [ ] `curl POST /api/skeleton/generate` 返回 SSE 流，最终 `JSON.parse` 成功得到 `{title, sections}`
- [ ] `curl POST /api/mentor/review` 返回 SSE 流，最终 `JSON.parse` 成功得到 `{items, summary, nextStep}`
- [ ] `curl POST /api/format/adapt` 返回 SSE 流，最终 `JSON.parse` 成功得到 `[{platform, title, content, notes}]`
- [ ] 文章 CRUD 四个接口均正常返回
- [ ] `GET /api/articles` 返回 camelCase 字段名（`bulletPoints` 非 `bullet_points`）

---

## P2：前端骨架 — 状态机 + 路由 + 服务层

### 目标

XState 状态机跑通全流程（用 mock 数据替代后端），路由切换正常，SSE 前端封装可用。

### 任务清单

1. **类型定义**
   - `src/state/appMachine.types.ts`：全部类型（BulletPoints、Skeleton、MentorFeedback、FormatPreview、ProgressEntry、WorkflowContext、WorkflowEvent）
   - 参照 `tect-design.md` §2.1

2. **状态机**
   - `src/state/appMachine.ts`：完整状态机代码
   - 包含所有 action 函数（clearAll、clearDraftAndFeedback、clearFeedback、clearError、logProgress）
   - 包含 guard（validateBulletPoints）
   - **暂时不注册 services**（先验证状态转移逻辑，P3 联调时再接入真实后端）
   - 参照 `tect-design.md` §2.2

3. **SSE 前端封装**
   - `src/services/sse.ts`：`fetchSSE` + `extractJSON`
   - 参照 `tect-design.md` §6

4. **Axios 实例**
   - `src/services/api.ts`：baseURL `/api`，拦截器处理 code!=0
   - 参照 `tect-design.md` §5.1

5. **usePersistedMachine Hook**
   - `src/hooks/usePersistedMachine.ts`
   - `machineServices` 中先注册 mock 版本的3个 service，确认状态机能跑完全流程
   - **Mock 数据定义**（3个 service 各返回符合类型结构的预设数据）：
     ```typescript
     // mock generateSkeleton → Skeleton
     { title: "测试文章：AI编程的深层逻辑", sections: [
       { title: "## 从错误中学习", purpose: "讲述实操中的失败经历", guidingQuestions: ["你第一次用AI写代码时犯过什么错？", "这个错误教会了你什么？"] },
       { title: "## 反常识洞察", purpose: "拆解对AI编程的误解", guidingQuestions: ["你发现了什么不符合常识的规律？"] }
     ]}
     
     // mock getMentorFeedback → MentorFeedback
     { items: [{ quote: "测试引用", problemType: "逻辑断裂", diagnosis: "段落间缺乏过渡", rewrite: "重写后的段落...", whyBetter: "增加了过渡句", priority: "P1" }],
       summary: "整体逻辑需加强段落衔接", nextStep: "补充过渡句后可定稿" }
     
     // mock adaptFormat → FormatPreview[]
     [{ platform: "微信公众号", title: "适配标题", content: "<p>HTML内容</p>", notes: "发布提示" },
      { platform: "知乎", title: "适配标题", content: "<p>HTML内容</p>", notes: "发布提示" }]
     ```
   - 参照 `tect-design.md` §2.3

6. **路由 + 页面骨架**
   - `App.tsx`：完整路由（参照 `tect-design.md` §10.2）
   - `WorkflowPage.tsx`：骨架（根据 `state.value.workflow` 渲染对应步骤组件）
   - `WarehousePage.tsx`、`StyleSkillPage.tsx`：占位页面

7. **通用组件**
   - `LoadingOverlay.tsx`、`ErrorBanner.tsx`、`ProgressIndicator.tsx`
   - 参照 `tect-design.md` §3.3

8. **状态机流程验证**
   - 在浏览器中点击"生成文章骨架"→ 状态从 `input` 转到 `skeletonReview`（mock数据）
   - 确认骨架审核 → 写作 → 导师审阅 → 终审 → 格式适配 → 完成，全流程状态转移正确
   - 确认 BACK_TO_INPUT / BACK_TO_SKELETON / BACK_TO_WRITING 回溯正常
   - 确认知识库 OPEN/CLOSE 并行状态不阻断主流程

### 使用 Skill

- **`investigate`**：状态机转移异常、类型错误、Hook 报错，立即用 investigate

### 验收标准

- [ ] `npm run dev` 启动无报错，TypeScript 编译通过
- [ ] 浏览器访问 `/workflow` 看到 InputStep 表单
- [ ] 填写3个字段（各≥20字）点提交，状态转到 `skeletonReview`，看到骨架卡片
- [ ] 从骨架审核一路点到完成，全流程7个步骤状态转移正确
- [ ] 回溯操作（BACK_TO_INPUT / BACK_TO_SKELETON / BACK_TO_WRITING）正常
- [ ] 页面刷新后状态恢复（localStorage 持久化生效）
- [ ] `/warehouse`、`/style-skill` 路由可访问

---

## P3：前后端联调 — 核心工作流组件

### 目标

前端组件全部实现，替换 mock 为真实后端调用，E2E 流程可走通。

### 任务清单

1. **WorkflowPage 接入真实后端**
   - 打开 `src/hooks/usePersistedMachine.ts`
   - 将 `machineServices` 中3个 mock 函数替换为真实 `fetchSSE` 调用：
     ```typescript
     const machineServices = {
       generateSkeleton: ({ context }: any) =>
         fetchSSE<Skeleton>('/api/skeleton/generate', context.bulletPoints),
       getMentorFeedback: ({ context }: any) =>
         fetchSSE<MentorFeedback>('/api/mentor/review', {
           draft: context.draft,
           skeleton: context.skeleton,
         }),
       adaptFormat: ({ context }: any) =>
         fetchSSE<FormatPreview[]>('/api/format/adapt', {
           draft: context.draft,
           title: context.skeleton?.title ?? '',
         }),
     };
     ```
   - 在文件顶部添加 `import { fetchSSE } from '../services/sse';` 和类型 import
   - 参照 `tect-design.md` §2.3 中 `machineServices` 的真实实现

2. **InputStep 组件**
   - 3个 TextArea + Form 验证（min: 20）+ 示例数据填入
   - 参照 `tect-design.md` §3.3 InputStep

3. **SkeletonReviewStep 组件**
   - 展示骨架标题 + 各段卡片 + 编辑弹窗（Modal）
   - 三个操作：确认/修改/打回
   - 参照 `tect-design.md` §3.3 SkeletonReviewStep

4. **WritingStep 组件**
   - TipTap 编辑器 + 左侧骨架引导问题侧边栏
   - 提交时 `editor.getText()` 提取纯文本
   - 参照 `tect-design.md` §3.3 WritingStep

5. **CoachResponseStep 组件**
   - 初稿折叠展示 + 全局总结 + 逐段反馈卡片 + 下一步建议
   - 三个操作：确认/修改文稿/重审骨架
   - 参照 `tect-design.md` §3.3 CoachResponseStep

6. **FinalReviewStep 组件**
   - 纯文本展示 + 确认定稿按钮（无回溯）
   - 参照 `tect-design.md` §3.3 FinalReviewStep

7. **FormattingStep + CompletedStep 组件**
   - 多平台 Tabs 预览（`dangerouslySetInnerHTML`）
   - 保存文章按钮（`api.post('/articles')`）+ 开始新文章 + 前往仓库
   - 参照 `tect-design.md` §3.3 FormattingStep、CompletedStep

8. **KnowledgeDrawer 组件**
   - 搜索 + 问答两个 Tab（组件内部管理状态，不经过状态机 invoke）
   - 参照 `tect-design.md` §3.3 KnowledgeDrawer/KnowledgeSearch/KnowledgeChat

9. **E2E 流程联调**
   - 确保从输入到保存文章的完整流程可走通
   - 验证 SSE 流式渲染（chunk 逐段展示）
   - 验证错误处理（网络断开、AI调用失败的 ErrorBanner + 重试）

### 使用 Skill

- **`investigate`**：任何组件渲染异常、API 调用失败、SSE 解析错误，立即 investigate
- **`qa`**：本阶段完成后，用 qa skill（Standard 级别）对核心工作流做一轮测试，发现并修复 bug

### 验收标准

- [ ] 从 `/workflow` 填写3个字段 → 骨架生成（SSE流式） → 审核骨架 → 写初稿 → 导师审阅（SSE流式） → 终审 → 格式适配（SSE流式） → 保存文章，全流程走通
- [ ] SSE 流式渲染可见（骨架逐段出现、反馈逐条出现）
- [ ] 回溯操作正常（BACK_TO_INPUT / BACK_TO_SKELETON / BACK_TO_WRITING）
- [ ] 页面刷新后状态恢复，可继续未完成的流程
- [ ] 知识库 Drawer 可打开/关闭，搜索和问答功能正常
- [ ] AI 调用失败时显示 ErrorBanner，点重试可恢复
- [ ] 保存文章成功后可在仓库中找到

---

## P4：知识库模块

### 目标

知识库后端服务完整实现，搜索+问答可用，根目录文件也能被加载。

### 任务清单

1. **知识库服务实现**
   - `services/knowledge_service.py`：`KnowledgeService` 完整实现
   - 包含：`_load_all`、`_extract_title`、`_chunk_article`、`search`、`_extract_snippet`、`ask`
   - 同义词映射 `SYNONYM_MAP`
   - **修复**：`_load_all` 需处理 `knowledge-base/` 根目录下的 `.md` 文件（当前代码跳过了根目录文件）
   - 参照 `tect-design.md` §9.2

2. **知识库路由**
   - `routers/knowledge.py`：搜索 + 问答
   - 启动时初始化 `KnowledgeService`，失败时优雅降级
   - 参照 `tect-design.md` §8.9

3. **依赖补充**
   - `requirements.txt` 添加 `jieba==0.42.1`（如尚未添加）

4. **验证**
   - curl 调用搜索接口，确认返回结果含 `matchType`
   - curl 调用问答接口，确认返回含 `answer` + `sources`
   - 确认根目录的2篇文章（`70 Vibe Coding 概念大全.md`、`90 Vibe Coding 常见问题和解决.md`）能被搜索到

5. **前端联调**
   - KnowledgeDrawer 的搜索和问答对接真实后端
   - 验证搜索结果展示（标题命中★ / 正文匹配 / 同义词关联）

### 使用 Skill

- **`investigate`**：知识库加载失败、搜索返回空、jieba 分词异常

### 验收标准

- [ ] 后端启动日志显示知识库加载完成（文章数 ≥ 60）
- [ ] `curl POST /api/knowledge/search {"query":"vibe coding提示词"}` 返回非空结果，含 `matchType`
- [ ] `curl POST /api/knowledge/ask {"question":"什么是vibe coding"}` 返回含 answer 和 sources
- [ ] 搜索 `Vibe Coding 概念大全` 能命中根目录的那篇文章
- [ ] 前端知识库面板搜索、问答正常工作

---

## P5：文章仓库 + 风格Skill

### 目标

仓库CRUD完整可用，风格Skill可用。

### 任务清单

1. **ArticleList 组件**
   - 文章列表 + 删除 + 空状态 + 跳转详情
   - **修复**：import 路径 `'../services/api'` → `'../../services/api'`
   - 参照 `tect-design.md` §3.3 ArticleList

2. **ArticleDetail 组件**
   - 完整文章详情（正文 + 骨架 + 导师反馈 + 格式预览）
   - **修复**：import 路径 `'../services/api'` → `'../../services/api'`
   - 参照 `tect-design.md` §3.3 ArticleDetail

3. **WarehousePage 组件**
   - 参照 `tect-design.md` §3.3 WarehousePage

4. **StyleSkillPage 组件**
   - 文章选择 + 风格生成 + 一键复制
   - 参照 `tect-design.md` §3.3 StyleSkillPage

5. **后端验证**
   - 确认 `POST /api/style-skill/generate` 可正常返回 `{stylePrompt}`
   - 确认文章 CRUD 接口返回 camelCase 字段

### 使用 Skill

- **`qa`**：本阶段完成后，用 qa skill（Standard 级别）对仓库和风格Skill做一轮测试

### 验收标准

- [ ] `/warehouse` 显示已保存文章列表，点击进入详情页
- [ ] 详情页展示正文、骨架、导师反馈、格式预览
- [ ] 删除文章功能正常，列表自动刷新
- [ ] 空仓库时显示"还没有文章"引导
- [ ] `/style-skill` 选择文章 → 生成风格 Skill → 复制到剪贴板
- [ ] 文章数量不足时显示提示

---

## P6：集成测试 + Bug修复 + 收尾

### 目标

全功能可用，无阻塞性bug，代码质量达标。

### 任务清单

1. **全流程E2E测试**
   - 从零开始走一遍完整工作流：输入 → 骨架 → 写作 → 导师 → 终审 → 格式 → 保存
   - 测试所有回溯路径
   - 测试边界情况：字数刚好20字、极长文本、网络中断恢复

2. **状态持久化测试**
   - 每个步骤刷新页面，确认状态恢复
   - 多次 RESET 后确认无内存泄漏

3. **TipTap 编辑器验证**
   - 确认 `getText()` 提取纯文本正常（含段落换行）
   - 确认回退到 writing 时 `setContent(draft)` 初始化正常

4. **格式预览验证**
   - 微信公众号/知乎两个 Tab 均正常渲染 HTML
   - `dangerouslySetInnerHTML` 渲染无报错

5. **安全收尾**
   - API Key 未配置时启动给出明确警告（非静默失败）
   - `.env` 在 `.gitignore` 中

6. **代码清理**
   - 移除所有 `console.log` 调试输出
   - 移除未使用的 import
   - 确认 TypeScript strict 模式无报错

### 使用 Skill

- **`qa`**：用 qa skill（Exhaustive 级别）对整个应用做完整测试
- **`investigate`**：qa 发现的任何 bug，用 investigate 做根因分析后再修

### 验收标准

- [ ] 全流程 E2E 无报错、无白屏、无数据丢失
- [ ] 所有回溯路径正常
- [ ] 页面刷新后状态恢复正常
- [ ] `npm run build` 构建成功（TypeScript 无错误）
- [ ] API Key 未配置时启动有明确提示
- [ ] qa 测试无 Critical/High 级别 bug 残留

---

## 开发纪律

1. **每完成一个阶段，先跑通该阶段全部验收标准，再进入下一阶段**
2. **任何报错不要猜测，用 `investigate` skill 做根因分析**
3. **每个阶段完成后用 `qa` skill 做一轮测试，发现 bug 当阶段修完**
4. **编码时严格参照 `tect-design.md` 的实现代码，不要自行发挥架构**
5. **`CLAUDE.md` 中标注的坑点（Skeleton是对象不是数组、draft全程纯文本、终审无回溯等）必须遵守**



