# P5 开发启动提示词

你是一个高级全栈工程师，正在开发「AI深度创作引擎」项目。目前 P0-P4 已完成，你需要开发 P5 阶段代码。每完成一个阶段的开发都要向我确认并请求是否开始下一阶段。此外，执行过程中遇到任何你认为需要向我询问或不清楚的事，都可以直接吩咐我。另外，积极使用联网搜索工具和context7这个MCP，避免模型知识库过时引起bug出现。

---

## 开始前先读这三份文档

按顺序读，它们是硬约束：

1. `CLAUDE.md` — 坑点必须遵守（Skeleton是对象不是数组、draft全程纯文本、终审无回溯等）
2. `tect-design.md` — 编码时严格参照其中的代码，不要自行发挥架构（注意：是参照不是照抄，具体编写情况以测试运行的结果为准）
3. `开发规划文档.md` — 流程控制，告诉你每个阶段做什么、参照什么、会用到哪些skill、验收标准是什么

**重要纪律：开发规划文档中"使用 Skill"部分不是建议，是必须执行的步骤。每阶段完成后对照验收标准逐条检查，该用qa就用qa，该用investigate就用investigate。**

---

## P0-P4 已完成的交付物

### 后端
```
backend/
├── main.py              # FastAPI入口，已注册6个路由 + CORS + health
├── config.py            # LLM_MODEL = "deepseek-v4-flash"（注意：已从v4-pro切到Flash）
├── database.py          # SQLAlchemy引擎+Session
├── models.py            # Article ORM模型
├── schemas.py           # 全部Pydantic请求/响应模型（含camelCase映射）
├── routers/
│   ├── skeleton.py      # POST /api/skeleton/generate (SSE) ✓已验证
│   ├── mentor.py        # POST /api/mentor/review (SSE) ✓已验证
│   ├── format.py        # POST /api/format/adapt (SSE) ✓已验证
│   ├── knowledge.py     # POST /api/knowledge/search + /ask ✓已验证（60篇文章，60 chunks）
│   ├── articles.py      # CRUD /api/articles ✓已验证
│   └── style_skill.py   # POST /api/style-skill/generate ✓已验证
├── services/
│   ├── llm.py           # stream_llm + call_llm (AsyncOpenAI)
│   ├── knowledge_service.py  # 关键词检索 + RAG问答 + 同义词映射（使用jieba分词）
│   └── prompt_templates.py   # 全部Agent提示词
└── data/app.db          # SQLite（gitignore）
```

### 前端
```
frontend/
├── src/
│   ├── main.tsx         # 入口（已引入@ant-design/v5-patch-for-react-19）
│   ├── App.tsx          # 路由+导航+ConfigProvider
│   ├── state/
│   │   ├── appMachine.ts         # XState状态机（5 action + 1 guard + invoke带input字段）
│   │   └── appMachine.types.ts   # 全部TypeScript类型定义
│   ├── hooks/
│   │   └── usePersistedMachine.ts # machine.provide({actors})注册服务 + localStorage持久化
│   ├── services/
│   │   ├── api.ts        # Axios实例（baseURL '/api'，拦截器返回res.data）
│   │   └── sse.ts        # fetchSSE<T>封装 + extractJSON降级解析
│   ├── pages/
│   │   ├── WorkflowPage.tsx    # 完整工作流（7步骤按状态渲染）
│   │   ├── WarehousePage.tsx   # 占位（P5实现）
│   │   └── StyleSkillPage.tsx  # 占位（P5实现）
│   ├── components/
│   │   ├── workflow/
│   │   │   ├── InputStep.tsx         # 3个TextArea + Form验证(min:20) + 示例填入
│   │   │   ├── SkeletonReviewStep.tsx # 骨架卡片 + 编辑弹窗(Modal)
│   │   │   ├── WritingStep.tsx       # TipTap编辑器 + 骨架侧边栏 + 提交验证(min:50字)
│   │   │   ├── CoachResponseStep.tsx  # 逐段反馈卡片(P0/P1/P2标签) + 全局总结
│   │   │   ├── FinalReviewStep.tsx    # 纯文本展示 + 终审无反回溯
│   │   │   ├── FormattingStep.tsx     # 双平台Tabs（微信公众号+知乎HTML预览）
│   │   │   └── CompletedStep.tsx      # 保存文章 + 前往仓库 + 开始新文章
│   │   ├── knowledge/
│   │   │   ├── KnowledgeDrawer.tsx    # Drawer + Tabs(搜索/问答)
│   │   │   ├── KnowledgeSearch.tsx    # 搜索列表 + matchType标签展示
│   │   │   └── KnowledgeChat.tsx      # 问答对话
│   │   ├── warehouse/          # P5实现
│   │   └── common/
│   │       ├── LoadingOverlay.tsx
│   │       ├── ErrorBanner.tsx
│   │       └── ProgressIndicator.tsx
│   └── styles/
│       ├── index.css    # Tailwind v4 (@import "tailwindcss")
│       └── tiptap.css
├── vite.config.ts        # Vite + proxy /api → localhost:8000
├── postcss.config.js     # @tailwindcss/postcss（注意：Tailwind v4语法）
└── package.json          # 所有依赖已安装
```

---

## 已知Bug修复记录（不要在新对话中重复踩坑）

1. **`validateBulletPoints` guard 检查 context 而非 event** — guard原来检查`context.bulletPoints`（始终为空），已修复为检查`event.bulletPoints`。`appMachine.ts`。

2. **XState v5.32 API 变化** — `useMachine`不接受`services`选项，已改用`machine.provide({ actors: { fromPromise(...) } })`。invoke需显式声明`input`字段传递context。

3. **`_load_all` 跳过嵌套子目录** — `knowledge-base/02-编程工具/工具实战/`下13篇文章未被加载。已修复：`glob→rglob`。

4. **WritingStep 空初稿提交** — 已添加50字最低验证，`message.warning`拦截。

5. **SSE onError 信息丢失** — 三个handler已改为`({event}) => ...${event.error?.message}`传递后端错误详情。

6. **Tailwind CSS v4 兼容** — `postcss.config.js`用`@tailwindcss/postcss`，`index.css`用`@import "tailwindcss"`语法。

---

## P5 待办（严格参照开发规划文档即D:\Automated Self-Media Content Creation Workflow\开发规划文档.md执行）

目标：文章仓库CRUD完整可用，风格Skill可用。

任务清单：
- ArticleList.tsx / ArticleDetail.tsx / WarehousePage.tsx / StyleSkillPage.tsx
- 注意：tect-design.md标注的import路径修正（`'../services/api'` → `'../../services/api'`）
- 完成后必须使用 **qa skill（Standard 级别）** 测试

---

## 启动命令

```bash
# 后端
cd backend && heartwrite-venv/Scripts/uvicorn main:app --reload --port 8000

# 前端
cd frontend && npm run dev
# → localhost:5173（如端口被占用会递增）

# pip安装走代理（如需要）
heartwrite-venv/Scripts/pip install --proxy http://127.0.0.1:7890 <package>
```
